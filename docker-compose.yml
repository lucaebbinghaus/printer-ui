version: "3.8"
services:
  printer-ui:
    image: printer-ui:local
    container_name: printer-ui
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - APP_DATA_DIR=/data
    volumes:
      - printer-ui-data:/data

volumes:
  printer-ui-data:
